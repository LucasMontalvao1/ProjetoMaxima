@model ProjetoMaxima.Models.Departamento

@{
    ViewData["Title"] = "Detalhes do Departamento";
}

<h2>@ViewData["Title"]</h2>

<div class="card">
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Codigo)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Codigo)
            </dd>
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Descricao)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Descricao)
            </dd>
        </dl>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model.Codigo" class="btn btn-primary">Editar</a>
    <a asp-action="Delete" asp-route-id="@Model.Codigo" class="btn btn-danger">Deletar</a>
    <a asp-action="Index" class="btn btn-secondary">Voltar para a listagem</a>
</div>
